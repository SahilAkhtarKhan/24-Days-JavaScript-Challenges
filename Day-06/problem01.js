// To find the largest pair of the 2 elements using indexing with sorted elements

function largestPair(array) {
  let res = `Largest pair is: ${array[array.length - 1]} ${
    array[array.length - 2]
  }`;
  console.log(res);
}
largestPair([1, 2, 3, 4, 5, 6, 7, 8, 9]);
